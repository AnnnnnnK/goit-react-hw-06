const Filter = () => {
  return (
    <input
      //   className={css.input}
      type="search"
      //   value={value}
      //   onChange={onChange}
      placeholder="Find name"
    />
  );
};

export default Filter;
